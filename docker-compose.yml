services:
  backend:
    build:
      context: ./backend
    ports:
      - "8080:8080"
    # depends_on:
    #   - db

  frontend:
    build:
      context: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend

  # db:
  #   # placeholder: конкретная СУБД будет выбрана и обоснована отдельно
  #   image: postgres:16-alpine
  #   ports:
  #     - "5432:5432"
  #   environment:
  #     POSTGRES_USER: app
  #     POSTGRES_PASSWORD: app
  #     POSTGRES_DB: app
  #   volumes:
  #     - db-data:/var/lib/postgresql/data

# volumes:
#   db-data:
